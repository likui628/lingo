type Props = {
  index: number;
  totalCount: number;
  current?: boolean;
  locked?: boolean;
  percentage: number;
}

export const LessonButton = ({}: Props) => {
  return (
    <div>LessonButton</div>
  )
}